# Mac OSX
.DS_Store
# Windows Custom Icons
desktop.ini

# Global environment variables and secrets
.env
config/api-keys.yaml
config/gateway.yaml
config/indexer.yaml

# Chimera config (secrets and local overrides, not for repo)
chimera/config/tokens.yaml
chimera/config/api-keys.yaml
chimera/config/gateway.yaml
chimera/config/indexer.yaml
chimera/config/bifrost.config.json

# Generated by `make catalog-free`, `make catalog-available`, and `make config-provider-free-tier`
config/catalog-free-tier.snapshot.yaml
config/catalog-available.snapshot.yaml
config/provider-free-tier.generated.yaml

# Packaging and release artifacts
dist/
/packaging/qdrant-bundles/

# Runtime / debug log files (not test fixtures)
temp/
/logs/
*.log
gw_out.txt
!**/*.example.log
!**/testdata/**/*.log

# Python bytecode
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Root binaries (chimera-* build outputs / package desktop copy)
bin/
chimera/bin/
locus/bin/

# Third-party binaries and sources (BiFrost, Qdrant)
.deps/
chimera/.deps/

# Chimera and locus configuration and runtime state (supervisor pid files, service data)
/data/
/run/
.data/
chimera/run/
chimera/data/
locus/run/
run/

# indexer sync data
config/indexer.sync-state.json

# SQLite runtime state
config.db
config.db-shm
config.db-wal

# Local model weights (large files)
models/

# Claude Code session state and worktrees
.claude/

# IDE extension config
.continue/

# Journal / personal data (orphaned from old Locus app)
Journal_Database/

# Moto X data folders, phone media, Syncthing state (scripts in phone-scripts/ are tracked)
Moto X/motox_audio_data/
Moto X/phone-scripts/motox_chunk.aac
Moto X/phone-scripts/motox_unsent/
Moto X/alt_autocycle/
Moto X/Alarms/
Moto X/Android/
Moto X/bluetooth/
Moto X/Download/
Moto X/Movies/
Moto X/Music/
Moto X/Notifications/
Moto X/Pictures/
Moto X/Podcasts/
Moto X/Ringtones/
Moto X/.stfolder
Moto X/.stversions/
Moto X/phone-scripts/motox_record_log.md
Moto X/phone-scripts/receiver_log.md
Moto X/receiver_log.md

# Windows launcher build artifact (resource.rc is tracked; .syso is compiled output)
chimera/cmd/porcelain/rsrc.syso
